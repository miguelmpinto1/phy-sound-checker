services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: phy-sound-checker
    environment:
      - PYTHONPATH=/app/src
    volumes:
      - .:/app          # edita no host, roda no container
    tty: true
    stdin_open: true
